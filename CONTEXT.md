# Agent O.S.A. context

`CNPJ` is the textual, validated identifier of a company. A company name or a
folder name is descriptive metadata and never proves ownership. Documents use
closed vocabularies for type and department; uncertain classification becomes a
review decision in later tasks rather than a guessed enum value.

The domain package is pure: it performs no database, filesystem, network, or
transport work.
